<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wayto.track">

    <!--允许程序打开网络套接字-->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--允许程序设置内置sd卡的写权限-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--允许程序获取网络状态-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--允许程序访问WiFi网络信息-->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!--允许程序读写手机状态和身份-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!--允许程序访问CellID或WiFi热点来获取粗略的位置-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".DataApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Wayto.AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TrackActivity"/>

        <service android:name=".service.LocationService" />

        <provider
            android:name=".provider.TrackProvider"
            android:authorities="com.wayto.track" />

        <provider
            android:name=".provider.TrackPointProvider"
            android:authorities="com.wayto.trackPoint" />

        <receiver android:name=".data.source.TrackRemote$TrackBroadcastReceiver">
            <intent-filter>
                <action android:name="com.wayto.track_refresh.view.action" />
                <action android:name="com.wayto.track_refresh.duration.action"/>
            </intent-filter>
        </receiver>

        <receiver android:name=".service.LocationService$TrackAlarReceiver">
            <intent-filter>
                <action android:name="com.wayto.alarm.action"/>
            </intent-filter>
        </receiver>

        <service android:name="com.amap.api.location.APSService" />

        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="2d567c6a24cd23107c99e0cc2247ff3a" />

    </application>

</manifest>